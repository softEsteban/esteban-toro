import NomadGame from "./NomadGame";

export const metadata = {
  title: "Nomad World · Esteban Toro",
  description: "Explore the nomad pixel world — collect Carb Claws across the map.",
};

export default function NomadGamePage() {
  return <NomadGame />;
}
